# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Envs
.venv/
venv/

# Tool caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS / editor
.DS_Store

# Fetched data snapshot cache (see scripts/fetch_snapshot.py). Ignored by default
# to avoid committing large/stale binaries; to *pin* a snapshot either keep this
# dir around, point BTF_CACHE_DIR at a shared copy, or force-add it intentionally.
data_cache/
